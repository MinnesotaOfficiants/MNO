<b>Editing Inquiry</b>  <%= @inquiry.wedding.get_title %> <br />

<% if @user.admin? %>
	<%= render 'admin_form', inquiry: @inquiry %>
<% else %>
	<%= render 'form', inquiry: @inquiry %>

<% end %>

<%= link_to 'Back', inquiries_path %>
