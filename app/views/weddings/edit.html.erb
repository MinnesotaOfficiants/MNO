<b>Editing Wedding
&nbsp&nbsp  <%= @wedding.get_title %></b>  <hr>
	<% if @user.admin? %>
		<%= render 'admin_form' %>
	<% else %>
  	<%= render 'form' %>
  <% end %>

<%= link_to 'Back', weddings_path %>